program spaceX;

fun void Launch(int r, int f, int j1); {
  var
  int a, b;
    a = 10;
    b = 20;
    b = a * b;
    print(b);
}

main() {
    var 
        int i, j, k, l, m, d, x, y, ggg[3];

        k = i + k;
        l = i - j;
        m = i * l;

      x = 10;

        if(x != j){
            d = x / y;
        }

       if (d == k) {
           print("son iguales");
       }

}
end